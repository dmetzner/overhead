{
  "name": "overhead",
  "version": "2.5.0",
  "private": true,
  "type": "module",
  "description": "Browser extension that injects HTTP request headers.",
  "scripts": {
    "test": "node --test",
    "lint": "biome check .",
    "format": "biome check --write .",
    "sync:share": "cp share.js docs/share.js",
    "check": "npm run sync:share && biome check . && node --test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5"
  }
}
